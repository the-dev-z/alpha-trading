services:
  # Backend service (API and core logic)
  alpha-trading:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.backend
    container_name: alpha-trading
    restart: unless-stopped
    stop_grace_period: 30s  # 允许应用有 30 秒时间优雅关闭
    ports:
      - "${ALPHA_BACKEND_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - AI_MAX_TOKENS=8000
    networks:
      - alpha-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Frontend service (static serving and proxy)
  alpha-frontend:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.frontend
    container_name: alpha-frontend
    restart: unless-stopped
    ports:
      - "${ALPHA_FRONTEND_PORT:-3000}:80"
    networks:
      - alpha-network
    depends_on:
      - alpha-trading
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  alpha-network:
    driver: bridge
